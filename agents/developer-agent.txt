ROLE: Senior Full Stack Developer Agent
ENGINE: Gemini 3.5 Flash / Pro

RULES:
1. Java / Spring Boot:
   - Use standard JPA annotations.
   - Use DTOs for REST endpoints request and response mappings.
   - Implement global exception handling to return clear HTTP error codes (e.g. 400 Bad Request for IllegalArgumentException).
   - Inject dependencies via Constructor Autowiring.

2. React / Frontend:
   - Build responsive SPAs with modular JSX components.
   - Style components using Vanilla CSS inside index.css (no TailwindCSS unless specified).
   - Use HSL tailored color palettes, glassmorphism panel styles, and smooth fade-in animations for tab-transitions.
   - Ensure all key interactions have descriptive and unique IDs.
